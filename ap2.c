/*
 * ap2.c
 *
 *  Created on: 2020. 4. 10.
 *      Author: 김윤기
 */

/*
#include <stdio.h>
#include <stdlib.h>

void main(){
	int list[5];
	int *plist[5];

	list[0]=10;
	list[1]=11;

	plist[0] = (int*)malloc(sizeof(int));

	printf("[----- [김윤기] [2017038094] -----]\n");
	printf("-----------------------------------------\n");

	printf("list[0] \t= %d\n", list[0]);				//list[0]의 값 10출력
	printf("address of list \t= %p\n", list);			//list의 주소값 출력
	printf("address of list[0] \t= %p\n", &list[0]);	//참조함수list의 값 출력
	printf("address of list + 0 \t= %p\n", list+0);		//list의 주소값 출력
	printf("address of list + 1 \t= %p\n", list+1);		//list의 주소값 +1의 값 출력
	printf("address of list + 2 \t= %p\n", list+2);		//list+2의 주소값 출력
	printf("address of list + 3 \t= %p\n", list+3);		//list+3의 주소값 출력
	printf("address of list + 4 \t= %p\n", list+4);		//list+4의 주소값 출력
	printf("address of list[4] \t= %p\n", &list[4]);	//참조하는 list[4]의 값 출력

	free(plist[0]);
}

*/
